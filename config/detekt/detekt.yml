build:
  maxIssues: 0

config:
  validation: true

processors:
  active: true

console-reports:
  active: true

output-reports:
  active: true

comments:
  active: false

complexity:
  active: true
  LongParameterList:
    active: true
    functionThreshold: 8

style:
  active: true
  MagicNumber:
    active: false
  MaxLineLength:
    active: true
    maxLineLength: 120

naming:
  active: true

potential-bugs:
  active: true

performance:
  active: true

empty-blocks:
  active: true

coroutines:
  active: true
